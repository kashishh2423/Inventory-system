# Database connection string
# For Postgres (Docker setup)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inventoryflow

# For SQLite local fallback development (leave blank to auto-fallback to sqlite:///./inventoryflow.db)
# DATABASE_URL=sqlite:///./inventoryflow.db

# Deployment environment
ENV=development
PORT=8000
